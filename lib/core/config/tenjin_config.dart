/// Tenjin SDK configuration
///
/// Replace the placeholder value with your actual Tenjin SDK key.
/// You can find this in Tenjin Dashboard > Organization tab
class TenjinConfig {
  /// Your Tenjin SDK Key
  /// Get this from: Tenjin Dashboard > Organization > SDK Key
  static const String sdkKey = 'YOUR_TENJIN_SDK_KEY';

  /// Enable debug mode for testing events
  /// Set to true during development, false for production
  static const bool debugMode = false;
}
